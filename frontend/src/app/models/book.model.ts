export interface Book {
  id: number;
  title: string;
  author: string;
  isbn: string;
  price: number;
  genre: string;
  description: string;
  coverImageUrl: string;
  rating: number;
  stock: number;
}
